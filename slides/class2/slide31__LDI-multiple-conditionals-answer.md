## Let's Develop It

Here is what your `dogAge` function should look like now:

```
cost dogAge = (age) => {
  if (age > 8) {
    return 'Elderly';
  } else if (age > 2) {
    return 'Adult';
  } else {
    return 'puppy';
  }
}
```